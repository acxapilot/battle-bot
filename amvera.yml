meta:
  environment: node
  toolchain: npm
build:
  requirements:
    - package.json
  artifacts:
    - index.js
    - database.db
run:
  command: npm install && node index.js
  persistence:
    - database.db
